import { PrespacePipePipe } from './prespace-pipe.pipe';

describe('PrespacePipePipe', () => {
  it('create an instance', () => {
    const pipe = new PrespacePipePipe();
    expect(pipe).toBeTruthy();
  });
});
